# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: False
  weight: 6
  showOnNavbar: False
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
